"""Experimental TissueResolve components (NOT imported by default).

Each subpackage is feature-flagged and isolated from the production pipeline.
Import explicitly, e.g. ``from tissueresolve.experimental.soft_hierarchy import
apply_partial_confidence_gating``.  Status labels live on each component's
config/result dataclass.
"""
